{% set name = "pyodide-py" %}
{% set version = "0.23.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pyodide-py-{{ version }}.tar.gz
  sha256: 77aeb62c656e76ea957588d2fe025e32b1619b2e2cf624b7fd0c531c0dfb849a

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.10
    - pip
  run:
    - python >=3.10

test:
  imports:
    - pyodide
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/pyodide/pyodide
  summary: '"A Python package providing core interpreter functionality for Pyodide."'
  license: MPL-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - yuvipanda
